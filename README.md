# SecureFlow Sarl

SecureFlow Sarl website and administration application.

## Development

Install dependencies and start the application:

    npm install
    npm run dev

## Environment

Configure the database and email settings through environment variables. Never commit credentials or secret values.
